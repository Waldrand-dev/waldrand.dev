{
  "name": "waldrand.dev",
  "version": "0.1.0",
  "private": true,
  "description": "waldrand.dev — the API surface site. Static, typed, no framework.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
